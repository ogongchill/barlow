auth:
  jwt:
    crypto:
      private-key: ${jwt.crypto.private-key}
      public-key: ${jwt.crypto.public-key}

---
spring.config.activate.on-profile: local

---
spring.config.activate.on-profile:
  - local-dev
  - dev

---
spring.config.activate.on-profile:
  - staging
  - live